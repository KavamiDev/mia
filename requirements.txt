# MIA — Assistante téléphonique restaurant
# Python 3.11+

# Framework web
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Base de données PostgreSQL
sqlalchemy==2.0.25
psycopg2-binary==2.9.9

# OpenAI Realtime API
openai==1.12.0
websockets>=12.0
certifi>=2024.0.0

# Configuration & validation
python-dotenv==1.0.0
python-multipart==0.0.9
pydantic==2.5.3

# Vérification signature Ed25519 des webhooks Telnyx
pynacl==1.5.0

# Dashboard admin (templates HTML)
jinja2==3.1.3

# Migrations DB
alembic==1.13.1

# Observability (optionnel — Sentry init dans main.py si SENTRY_DSN défini)
sentry-sdk[fastapi]==1.40.0

# --- Tests (dev seulement) ---
# pip install -r requirements-dev.txt suffit, mais on garde tout ici pour simplicité.
pytest==8.0.0
httpx==0.26.0       # Backend HTTP pour fastapi.testclient
